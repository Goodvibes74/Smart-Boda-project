import 'package:flutter/material.dart';

class MapOverlayWidget extends StatelessWidget {
  const MapOverlayWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Map Overlay Here'),
    );
  }
}